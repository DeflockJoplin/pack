This directory is intentionally empty in the public source distribution.

Build the dashboard before running pack:

  cd frontend
  npm ci
  npm run build

Output lands here (index.html + assets/). See docs/FRONTEND_DEVELOPMENT.md.
